<div class="row cart_items">
	<h2>カート一覧</h2>
	 <table class="table table-hover table-inverse">

		<thead>
	      <tr>
  			<th></th>
  			<th>商品名</th>
  			<th>単価(税込)</th>
  			<th>数量</th>
  			<th>小計</th>
  			<th></th>
  			<th colspan="3"></th>
  		  </tr>
		</thead>

		<tbody>
　　　　　　<p class="text-right">
			<%= link_to "全て削除", destroy_all_cart_items_path(@cart_items), class: "btn btn-danger", method: :delete %>
		  </p>
			<% @cart_items.each do |cart_item| %>
			<tr>
				<td><%= attachment_image_tag cart_item,:image %></td>
				<td><%= cart_item.name %></td>
				<td><%= cart_item.price %></td>
　　　　　　　　　　<td><%= cart_item.select :quantity, options_for_select(@cart_items, {selected: cart.quantity.to_i} ) %></td>
	            <td><%= cart_item.total_price %>
                </td>
				<td><%= link_to "削除", destroy_cart_items(cart_items),class: "btn btn-danger", method: :delete %></td>
			</tr>
            <% end %>

        </tbody>
	  </table>
	      <div class="text-right">
          	<h2>合計金額</h2>	
          </div>

　　　　　　<p class="text-center">
			<%= link_to "買い物を続ける", products_path, class: "btn btn-primary btn-block " %>
		  </p>

		  <p class= "text-center">
		    <%= link_to "情報入力に進む", new_order_path, class: "btn btn-success btn-block" %>
          </p>
          
</div>